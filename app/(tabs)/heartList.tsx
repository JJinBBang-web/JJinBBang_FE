import { View, Text } from 'react-native';

export default function HeartListScreen() {
  return (
    <View style={{ flex: 1, alignItems: 'center', justifyContent: 'center' }}>
      <Text>관심 목록</Text>
    </View>
  );
}